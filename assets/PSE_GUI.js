/**
 * PSE_GUI.js
 *
 * This script draws the Periodic Table of Elements (ger. PSE = Periodensystem der Elemente).
 */
//document.addEventListener("DOMContentLoaded", () => PSE_GUI_init(), false);
document.addEventListener("DOMContentLoaded", function () { PSE_GUI_init(); }, false);

var PSE_GUI = (function () {
	return {
		init : function () {
			var mTable = document.createElement("TABLE");
		}
	};
})();